package tanks.model;

import java.awt.image.BufferedImage;

public class Tank extends GameObject
{

	public Tank(BufferedImage img, int x, int y, int z, boolean so, int sp)
	{
		super(img, x, y, z, so, sp);
	}

}
